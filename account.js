const tabs=document.querySelectorAll('.auth-tab');const title=document.querySelector('#authTitle');const copy=document.querySelector('#authCopy');const submit=document.querySelector('#authSubmit');const nameField=document.querySelector('#nameField');let mode='login';tabs.forEach(tab=>tab.addEventListener('click',()=>{mode=tab.dataset.mode;tabs.forEach(t=>t.classList.toggle('active',t===tab));const signup=mode==='signup';nameField.classList.toggle('hidden',!signup);document.querySelector('#name').required=signup;title.textContent=signup?'Create your account.':'Welcome back.';copy.textContent=signup?'Start managing your domains and hosting in one simple place.':'Log in to manage your domains, hosting, and billing in one place.';submit.innerHTML=signup?'Create account <span>→</span>':'Log in <span>→</span>'}));document.querySelector('#authForm').addEventListener('submit',e=>{e.preventDefault();localStorage.setItem('domainly-user',JSON.stringify({name:document.querySelector('#name').value||'Alex Morgan',email:document.querySelector('#email').value}));window.location.href='dashboard.html'});document.querySelector('#forgot').addEventListener('click',e=>{e.preventDefault();alert('Password reset will be connected when the backend is added.')});
